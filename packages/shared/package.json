{
  "name": "@repo/shared",
  "type": "module",
  "scripts": {
    "dev": "astro sync && tsc --watch",
    "build": "astro sync && tsc"
  },
  "dependencies": {
    "@repo/ui": "workspace:*",
    "@repo/utils": "workspace:*",
    "botid": "^1.5.10"
  }
}
