{
  "name": "@repo/shared",
  "type": "module",
  "scripts": {
    "dev": "astro sync && tsc --watch --noEmit",
    "build": "astro sync && tsc --noEmit"
  },
  "dependencies": {
    "@repo/ui": "workspace:*",
    "@repo/utils": "workspace:*"
  }
}
