---
import VideoRenderer, { VideoDataQuery, type VideoDataProps } from '@repo/ui/Video'

export const Video_Query = `
  _type == "Video" => ${VideoDataQuery('@')},
`

type Props = {
  node: VideoDataProps
}

const { node } = Astro.props
---

<VideoRenderer {...node} />
