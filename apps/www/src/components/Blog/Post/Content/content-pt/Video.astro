---
import VideoRenderer, { VideoDataQuery, type VideoDataProps } from '@repo/ui/Video.astro'

export const Video_Query = `
  _type == "Video" => ${VideoDataQuery('@')},
`

type Props = {
  node: VideoDataProps
}

const { node } = Astro.props
---

<VideoRenderer {...node} />
